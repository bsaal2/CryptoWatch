const wishlist = require('./wishlist-validator');
const crypto = require('./crypto-validator');
const notification = require('./notification-validator');

module.exports = {
    crypto,
    wishlist,
    notification
};
